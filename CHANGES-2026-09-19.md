# Changes, 19 Sep 2026 (fresh from the repo zip)

Web first alongside mobile, with your three real web applications as case studies 05 to 07. All copy lives in `src/content/`.

- `src/content/profile.ts`: title "Software Engineer · Web & Mobile", hero copy, proof rail (MSc added), about (London), final CTA, site name/url `https://sadman.tech`, description.
- `src/content/sections.ts`: service 1 proofs now DentXpert + Grocs; services 5 (web applications with background work, security and explainable AI) and 6 (multi tenant SaaS with roles, audit logs and public pages); new "Web" capability group; timeline: Devco Oct 2024 – Jul 2026, MSc Advanced Computer Science (GCU London) added, BSc dated, independent work described as four years of web and mobile.
- `src/content/projects/policywatch.ts`, `scamlens.ts`, `incidentkit.ts` (new, stations 05–07), written from your project documents and the screenshots; live links to policywatch.sadman.tech and scamlens.sadman.tech. IncidentKit has an empty `links` array: add its URL there when it is deployed.
- `src/content/projects/naqiverse.ts`: Download on the App Store link (apps.apple.com/my/app/naqiverse/id6779031756).
- `src/content/projects.ts`, `types.ts`: seven rooms, `grocs.next` → policywatch, `links` on projects, `BROWSER_ROOMS`.
- React Bits, one new per room, following the existing pattern: chapter backgrounds in `effects.tsx` (PolicyWatch: DotField, ScamLens: MagnetLines, IncidentKit: PlasmaWave); delights in `home/delights.tsx` (PolicyDiff on DecryptedText, ScamLookAlike on Shuffle, IncidentClock on CountUp); diagrams in `case/diagrams.tsx`; chapter config in `work-chapter.tsx`; room accents and browser frame styles in `globals.css`.
- `src/components/ui.tsx`: web rooms render in a browser frame instead of a phone. `case-page.tsx`: live and store links.
- `src/app/about/page.tsx`, `src/app/resume/page.tsx`, `site-header.tsx`: London, MSc, Devco end date, seven case studies, new title.
- `src/content/media.gen.ts`, `public/media/{policywatch,scamlens,incidentkit}/*`, `public/media/og/*.png`: your screenshots, cropped for the case pages.
- `public/CNAME`: sadman.tech. `tests/unit/content.test.ts`: seven projects, live domain.

Checks: `pnpm exec tsc --noEmit` clean, `pnpm exec eslint .` clean, `pnpm exec vitest run` 40 passed / 1 skipped, `pnpm exec next build` succeeds (16 static pages).

Not changed: the 3D hero still shows three phone screens.

## Round 3 (same day, later): React Bits per room, bigger browser cards

- Hero headline: "Web and Mobile products, realtime systems and AI, built end to end".
- DentXpert delight: React Bits **CallChip** (vendored, inline icons) reading `yolov8 · detecting caries` with a live timer; cycles running → done → queued. Caption changes with state.
- AIMARA delight rewritten: the packet only ever travels along the connectors and never over the box labels; inside a box the box lights up instead. Boxes are clickable (and keyboard operable): tapping one starts the packet there, it completes a full loop, then the normal back and forth resumes.
- ScamLens delight: 14 real indicator examples (look-alike characters, deceptive subdomains, IP hosts, punycode, sender vs reply-to, urgency, gift cards, DKIM/SPF failures…) auto-cycling every 3.4 s with a Shuffle transition; tap for the next one.
- PolicyWatch: background is React Bits **RippleDistortion** over the monitors screenshot (grayscale, blue tint and glint on hover, screen-blended so it never darkens the room); delight is a stack of React Bits **SwipeToast**s, "Caution: Policy changed for …" with the actual change as the description, swipe down to dismiss, new one every 3.2 s.
- IncidentKit: background is React Bits **SideRays** (mint and coral from the top right); delight is React Bits **FolderFloat** labelled "Incidents · 5 open" whose pills are incident names (INC-0042 Checkout returning 500s, …) with matter-js physics drift and drag.
- Browser rooms (05–07) now get a 600 × 404 CardSwap (was the 252 px phone stack), a wider right column, stacked full-width frames below `lg`, and a wider hero frame on their case pages. All 13 web screenshots re-cut to a consistent 16:10 1600 × 1000 frame (short captures extended with the page background instead of stretched), lightly sharpened.
- New vendored files: `src/components/vendor/{CallChip,SwipeToast,FolderFloat,SideRays,RippleDistortion}.tsx` (CSS in globals.css). New dependency: `matter-js` (+ `@types/matter-js`).
- Checks: `tsc` clean, ESLint clean, vitest 40 passed / 1 skipped, `next build` 16 pages.

## Round 4: compact delight slots, MoltenMetal, stable layout, timeline order

- PolicyWatch background is now React Bits **MoltenMetal** (frost palette, mouse drift) instead of RippleDistortion; the ripple vendor file was removed.
- Every chapter delight lives in a fixed 104 px tall `.delight-slot` to the right of "Read the case study", vertically centred on the button. Nothing wraps under the button and nothing changes the row's height.
- PolicyWatch toasts are absolutely positioned inside a fixed 320 × 76 px stack (newest in front, previous one tucked behind at 94 %), so arriving and dismissing toasts never move the page. Verified: the next section's position stayed identical across a full toast cycle.
- Grocs: a single 300 px chip flow with a fixed 58 px chip area; matching reorders the chips in place (pantry first, "→" marks what to buy) with a one-line status and a small MATCH PANTRY / RESET control.
- ScamLens: 300 px single-line example with truncated hint and counter; AIMARA figure fixed at 300 px with a one-line caption.
- IncidentKit folder shrunk to 150 × 84 with shorter incident names; the fan still floats above on hover without affecting layout.
- "The path so far" order: Independent (ongoing) → MSc (2026–27) → Devco → BSc → Published research → JIIICaS Gold Award.
- Checks: `tsc` clean, ESLint clean, vitest 40 passed / 1 skipped, `next build` 16 pages.
